import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* Enforce React Strict Mode for deep runtime performance monitoring */
  reactStrictMode: true,
};

export default nextConfig;